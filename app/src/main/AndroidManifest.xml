<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.george.vector">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Palladium"
        android:dataExtractionRules="@xml/data_extraction_rules">

        <service
            android:name=".notifications.FirebaseService"
            android:exported="false"
            android:permission="com.google.android.c2dm.permission.SEND">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
            </intent-filter>

        </service>

        <activity
            android:name=".develop.DevelopJavaActivity"
            android:exported="false" />
        <activity
            android:name=".develop.DevelopKotlinActivity"
            android:exported="false" />
        <activity
            android:name=".users.user.main.FolderUserActivity"
            android:exported="false" />

        <activity
            android:name=".auth.LoadingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".common.tasks.images.ImageTaskActivity" />
        <activity android:name=".users.root.main.MainRootActivity" />
        <activity android:name=".users.executor.tasks.EditTaskExecutorActivity" />
        <activity android:name=".common.settings.SettingsActivity" />
        <activity android:name=".users.executor.tasks.TaskExecutorActivity" />
        <activity android:name=".users.executor.main.ExecutorTasksActivity" />
        <activity android:name=".users.executor.main.FolderExecutorActivity" />
        <activity android:name=".users.root.tasks.EditTaskRootActivity" />
        <activity android:name=".users.root.tasks.TaskRootActivity" />
        <activity android:name=".users.root.tasks.AddTaskRootActivity" />
        <activity android:name=".users.root.folders.FolderRootActivity" />
        <activity android:name=".users.root.folders.LocationFolderActivity" />
        <activity android:name=".common.edit_users.EditDataUserActivity" />
        <activity android:name=".users.root.profile.EditUserActivity" />
        <activity android:name=".users.root.profile.ListUsersActivity" />
        <activity android:name=".users.user.tasks.TaskUserActivity" />
        <activity android:name=".users.user.tasks.AddTaskUserActivity" />
        <activity android:name=".users.executor.main.MainExecutorActivity" />
        <activity android:name=".users.user.main.MainUserActivity" />
        <activity android:name=".users.root.profile.RegisterUserActivity" />
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".users.root.profile.ProfileRootActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>